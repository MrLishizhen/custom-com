import { ListProps } from '@/script/index';

const list: ListProps[] = [
  {
    name: '测试列表1',
    img: '',
    component: '',
  },
];

// glob.map(async (u: any) => {
//   const data = await u();
//   console.log(data);
// });
// for (const key in glob) {
//   console.log(glob[key]().then((res) => console.log(res)));
// }

export default list;
